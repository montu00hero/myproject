

finding ancesstor
1)parent()  --it will give the direct parent of the element
2)parents() --it will return the all parent elements,and we can get the particular parent by passing its name as args.
3)parentUntil()--it will return all the parent between the two elements

finding desentdent
1)children() ----The children() method returns all direct children of the selected element.

                        This method only traverse a single level down the DOM tree.
2)find() ---- The find() method returns descendant elements of the selected element, 
                all the way down to the last descendant.* -for all elements.
